CREATE TABLE STUDENT (ID NUMBER(10) NOT NULL, LASTNAME VARCHAR NOT NULL, MEAN NUMBER(12,5), NAME VARCHAR NOT NULL, NBABSENCES NUMBER(10), GROUP_ID NUMBER(10), PRIMARY KEY (ID))
CREATE TABLE GROUPE (ID NUMBER(10) NOT NULL, NOM VARCHAR NOT NULL UNIQUE, PRIMARY KEY (ID))
ALTER TABLE STUDENT ADD CONSTRAINT FK_STUDENT_GROUP_ID FOREIGN KEY (GROUP_ID) REFERENCES GROUPE (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMBER(19), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
